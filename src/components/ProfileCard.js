import React from 'react';

const ProfileCard = (props) => (
    <div className="component-ProfileCard panel panel-default">
        <div className="panel-body">
            <h4>Profile Card</h4>
        </div>
    </div>

);

export default ProfileCard;